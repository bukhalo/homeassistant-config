# action.yml
name: 'Validate Home Assistant config'
description: ''
inputs:
  ha-version:
    description: 'Home Assistant Verison'
    required: true
    default: 'latest'
outputs:
  time:
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: '${{ inputs.ha-version }}'
  args:
    - ${{ inputs.ha-version }}
